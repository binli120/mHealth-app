# Semgrep ignore rules
# See: https://semgrep.dev/docs/ignoring-files-folders-code/

# Health check sidecar — internal-only deployment tool.
# All http.request / http.get / http.createServer calls target either:
#   • /var/run/docker.sock  (Unix domain socket, not a network interface)
#   • Internal Docker bridge IPs (172.x.x.x) that never leave the host
# This file is never exposed to the public internet (no host port binding).
deploy/healthcheck-sidecar/index.js
