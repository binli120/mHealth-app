/**
 * @author: Bin Lee
 * @email: blee@healthcompass.cloud
 */

// Vitest mock — 'server-only' is a Next.js compile-time guard that has no
// runtime behaviour. Exporting nothing here prevents import errors in tests.
export {}
